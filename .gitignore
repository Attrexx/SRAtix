node_modules/
dist/
.next/
*.log
*.tsbuildinfo
tmp/
.DS_Store
Thumbs.db

# Root lockfile is generated on deploy by postinstall; don't track it
/package-lock.json

# Secrets
.env
.env.*
!.env.example

# Credentials files — must never be committed to version control
attrexx-account.txt
*.credentials.txt
*.secrets.txt

/generated/prisma

# Runtime PID file (written by main.ts, read by pre-start.js)
.sratix.pid
