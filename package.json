{
  "name": "sratix",
  "version": "0.2.5",
  "private": true,
  "description": "SRAtix — Root orchestrator for Infomaniak deployment",
  "scripts": {
    "postinstall": "cd Server && npm install && npx prisma generate && cd ../Dashboard && npm install",
    "build": "cd Server && npm run build && cd ../Dashboard && npm run build",
    "start": "cd Server && node scripts/pre-start.js && node dist/main.js"
  }
}
